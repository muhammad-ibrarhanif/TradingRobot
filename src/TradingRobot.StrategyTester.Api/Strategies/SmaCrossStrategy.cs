// SmaCrossStrategy moved to the shared TradingRobot.Strategies library so every
// service (Strategy Tester, Signal Generator, Live Execution Bot, and
// Dashboard.Web's historical signal computation) references the same
// implementation instead of each keeping its own copy — see
// src/TradingRobot.Strategies/SmaCrossStrategy.cs.
//
// This file is a placeholder rather than deleted: the original file is locked by
// an open Visual Studio instance on this machine and can't be removed from here.
// Safe to delete manually once VS is closed — nothing references this file or
// this (now-empty) namespace anymore.
